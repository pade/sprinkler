language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pipenv install --dev

# command to run tests
script:
  - make check

branches:
  only:
  - master

notifications:
  email:
    - pdassier@free.fr
